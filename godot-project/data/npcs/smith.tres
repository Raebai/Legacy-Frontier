[gd_resource type="Resource" script_class="NPCData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/NPCData.gd" id="1_npc_data"]

[resource]
script = ExtResource("1_npc_data")
npc_id = "smith"
npc_name = "the quartermaster"
display_color = Color(0.85, 0.78, 0.42, 1)
lines = Array[String](["everything there takes something back.", "hammer hits harder. and slower.", "helmet buys hits, not speed.", "an empty slot is a choice.", "i sharpen lines, not blades.", "that one came back bent."])
