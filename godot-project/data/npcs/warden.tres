[gd_resource type="Resource" script_class="NPCData" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/NPCData.gd" id="1_npc_data"]

[resource]
script = ExtResource("1_npc_data")
npc_id = "warden"
npc_name = "the warden"
display_color = Color(0.55, 0.80, 0.72, 1)
lines = Array[String](["guard early and you eat it.", "the dash has real invulnerability.", "casts no longer chain. wait.", "everything is drawn before landing.", "nothing in the ring is permanent.", "friendly fire is on. everywhere."])
